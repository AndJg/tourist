import React, { Fragment } from 'react';

import './Hero.scss';
import Slider from './Slider';
const HeroSection = () => {
    return (
        <Fragment>
            <Slider />
        </Fragment>
    );
};

export default HeroSection;
