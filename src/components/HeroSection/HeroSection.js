import React, { Fragment } from 'react';

import Slider from './Slider';
const HeroSection = () => {
    return (
        <Fragment>
            <Slider />
        </Fragment>
    );
};

export default HeroSection;
